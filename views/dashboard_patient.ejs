<!doctype html><html><body>
<h2>Patient Dashboard</h2>
<h3>Upcoming</h3>
<ul>
<% upcoming.forEach(a=>{ %>
  <li><b><%= a.date %></b> — <%= a.doc_name %> — serial <%= a.serial_no %> —
    <a href="/appointments/<%= a.id %>/status">Live Status</a></li>
<% }) %></ul>
<h3>Past</h3>
<ul>
<% past.forEach(a=>{ %>
  <li><b><%= a.date %></b> — <%= a.doc_name %> — serial <%= a.serial_no %> —
    <a href="/consultations/appointment/<%= a.id %>">View Prescription</a></li>
<% }) %></ul>
</body></html>
