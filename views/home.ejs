<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Nirnoy</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <%- include('_header') %>
    <%- include('_flash') %>
    <div class="wrap">
      <% if (user) { %>
        <p>Signed in as <b><%= user.name||user.email %></b> (<%= user.role %>) — <a href="/logout">Logout</a></p>
      <% } else { %>
        <p>Not signed in — <a href="/login">Login</a> · <a href="/register">Patient Sign-up</a> · <a href="/doctor/register">Doctor Sign-up</a></p>
      <% } %>
      <div class="row">
        <div class="card" style="flex:1 1 280px">
          <h3>Patients</h3>
          <p>Find doctor → pick slot → live ETA → prescription/history.</p>
          <p><a href="/doctors">Browse doctors</a></p>
        </div>
        <div class="card" style="flex:1 1 280px">
          <h3>Doctors</h3>
          <p>Set weekly schedule. See queue. Finish with notes/prescription.</p>
          <p><a href="/doctor/dashboard">Doctor dashboard</a></p>
        </div>
        <div class="card" style="flex:1 1 280px">
          <h3>Admin</h3>
          <p>Verify BMDC + approve doctors.</p>
          <p><a href="/admin/doctors">Admin panel</a></p>
        </div>
      </div>
    </div>
    <%- include('_footer') %>
  </body>
</html>
