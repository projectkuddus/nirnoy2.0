<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Appointment Status</title>
    <meta http-equiv="refresh" content="30">
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <%- include('_header') %><%- include('_flash') %>
    <div class="wrap">
      <h2>Appointment Status</h2>
      <p><b>Doctor:</b> <%= appt.doctor_name %> &mdash; <b>Date:</b> <%= appt.appt_date %> &nbsp; <b>Time:</b> <%= appt.slot_time %></p>
      <div class="card">
        <% if(appt.status==='queued'){ %>
          <h3>⏳ You’re in the queue</h3>
          <p>Please stay nearby. This page auto-updates.</p>
        <% } else if(appt.status==='called'){ %>
          <h3>📣 It’s your turn</h3>
          <p>Proceed to <b>Room <%= appt.room||'—' %></b>. Show this screen if needed.</p>
        <% } else if(appt.status==='in_progress'){ %>
          <h3>🩺 With the doctor</h3>
          <p>Your consultation is in progress.</p>
        <% } else if(appt.status==='done'){ %>
          <h3>✅ Finished</h3>
          <p>Download your prescription from the appointment page.</p>
        <% } else if(appt.status==='no_show'){ %>
          <h3>⚠️ Marked as no-show</h3>
          <p>You can book a new slot from the doctor’s page.</p>
        <% } %>
      </div>
      <p><a href="/patient/dashboard">Back to dashboard</a></p>
    </div>
    <%- include('_footer') %>
  </body>
</html>
