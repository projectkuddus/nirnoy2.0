<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= doctor.name %> - Nirnoy</title>
  </head>
  <body>
    <h1><%= doctor.name %></h1>
    <p>Specialty: <%= doctor.specialty %></p>
    <p>Chamber: <%= doctor.chamber %></p>
    <p>Visit duration: <%= doctor.visit_duration_minutes %> minutes</p>

    <% if (!user) { %>
    <p><a href="/login">Login</a> to book an appointment.</p>
    <% } else { %>
    <h2>Book an Appointment</h2>
    <form method="POST" action="/appointments/book">
      <input type="hidden" name="doctor_id" value="<%= doctor.user_id %>" />
      <label>
        Date
        <input type="date" name="date" required />
      </label>
      <br />
      <label>
        Slot time
        <input type="time" name="slot_time" required />
      </label>
      <br />
      <label>
        For
        <input type="text" name="for_person_name" placeholder="Self or family member" />
      </label>
      <br />
      <button type="submit">Book Serial</button>
    </form>
    <% } %>
  </body>
</html>
